/*
 * Номер счётчика Яндекс.Метрики — единственное место, где он задан.
 *
 * Вынесен в обычный модуль намеренно: layout.js рендерится на сервере,
 * а Metrika.jsx помечен 'use client'. При импорте значения из клиентского
 * модуля в серверный React отдаёт ссылку-заглушку, и в разметку попадает
 * ym([object Object], 'init', ...) вместо номера.
 *
 * Тот же номер прописан в script.js лендинга /ai/ — счётчик общий,
 * иначе переход между главной и лендингом считался бы двумя визитами.
 */
export const METRIKA_ID = 111030011
